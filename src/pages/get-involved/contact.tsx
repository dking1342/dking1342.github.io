import React from 'react';
import GetInvolvedContact from '@/components/GetInvolvedContact';

type Props = {};

const contact = (props: Props) => {
  return <GetInvolvedContact />;
};

export default contact;
