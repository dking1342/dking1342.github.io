import Head from 'next/head';

type Props = {};

const About = (props: Props) => {
  return (
    <section>
      <Head>
        <title>About</title>
      </Head>
      <h1>About page</h1>
    </section>
  );
};

export default About;
